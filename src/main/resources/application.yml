#spring.application.name=InternshipPlatform
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/internship_db
    username: root
    password: Tresorhenri17!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop   # create  tables on startup, drops on shutdown
                              #update change ddl to update once tables are stable
                              #validate | none
    show-sql: true
    properties:
      hibernate:
        format_sql: true      #Makes the printed SQL readable

  sql:
    init:
      mode: always