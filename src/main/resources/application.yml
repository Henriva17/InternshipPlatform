#spring.application.name=InternshipPlatform
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/internship_db
    username: root
    password: Tresorhenri!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create | update | validate | none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  sql:
    init:
      mode: always